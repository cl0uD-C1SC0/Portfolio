#
#
#
Subi uma instancia para testar a aplicacao do github


git clone http://repisotiryurl.com

Instalei o PIP como root
yum install -y pip
cd repo/
pip3 install requeriments.txt
pip3 install flask
pip3 install outros-modulos-que-vai-pedir

Primeiro testei a comunicação da aplicação com o SSM Parameter Store (BACKEND_API)
python3 tests_app.py -v

# Notei que era para estar com 4 Status Code OK
# Porém só dois o "/" e o "/healtcheck"
# O Problema está na comunicação com o BACKEND

------------
# Verifique a URL DO BACKEND_API
# OBS: A URL tem que começar com http://

Dúvidas:

- O Valor do BACKEND_API estara certo no dia?
- O Valor do BACKEND_API existe?
- Onde eu acho o valor do BACKEND_API na prova?
- Pelo visto o codebuild vai testar a aplicação com o tests_app.py e se os 4 parametros derem OK, ele vai validar essa TASK.

- Mas o parametros são:
	/
	/healthcheck
	/unicorn
	/unicorn/some-unicorn

	O / e o /healthcheck funcionam, porém o /unicorn e /unicorn/some-unicorn não, pesquisei pelo o ERRO:

	"Caused by NewConnectionError" -> A URL não existe ou 
	"I've got the same problem, here are to ways to solve it: 1. the python library 	'requests' has a default setting to keep every session alive, write like 	following to disconnect the session: s=requests.session(), s.keep_alive = 	False; 2. add a parameter into requests: headers={'Connection':'close'}, req=requests(url, headers)"


# OBS:

	> Problemas no CI/CD
		- Buildspec com código faltando
		- Antes de fazer o CI/CD teste a aplicação que tem tests_app.py e 		verifique se rodou os 4  "/" OK
		- Verifique o BACKEND_API
		- Oque é a possess a valid auth secret?
