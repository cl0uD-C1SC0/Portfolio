#
#
#
TASK:
    1 - Go to Cognito User Pool
    2 - Navigate to Attributes sections and confirm if the rank and mission_name custom attributes are present
    3 - Use the AdminUpdateUserAttribues CLI command to update the values for the attributes. Make sure to use the --region parameter while running the command to specify the aws region in which
    the UserPool is present
    4 - Login again with the user as perfomed in Task 1 and confirm that the ID token now contains these two custom attributes. You can user https://jwt.io/ website to validate the Id token. 



TASK 1:
    CLI:
    aws cognito-idp admin-update-user-attributes --user-pool-id "value" --username "value" --user-attributes Name="custom:mission_name",Value="neptunespear" Name="custom:rank",Value="colonel" --region "region"


ANSWER:
    username of user