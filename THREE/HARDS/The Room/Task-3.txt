#
#
#
Crie um task definition:

TASK: 
    - Fargate Task definition
    - Image: traefik:v2.5 (or newer image)
    - Add two port mappings:
        tcp:80
        tcp:8080
    
    - HealthCheck command:
        CMD-SHELL, /usr/bin/wget --spider http://localhost:8080/ping || exit 1

    - Environment section, set the Command to be:
    --log.level=DEBUG,--providers.ecs=tru,--providers.ecs.autoDiscoverCluster=false,providers.ecs.cluster=roomWhereItHappens,--providers.ecs.exposedByDefault=false,--entryPoints.http80.address=:80,--api.dashboard=true,--api.insecure=true,--ping=true

    - Docker Labels as key/value pair.

    Key: traefik.enable Value true

OBS:
    Nome do container: traefik
    Imagem: traefik:v2.5

